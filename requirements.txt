# Core API dependencies (Render deployment)
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.8.0
python-multipart
httpx>=0.28.0
reportlab
jinja2

# Document handling
pypdf

# Translation / language detection
langdetect

# Local SLM fallback (optional — not used in cloud API mode)
transformers>=4.51.0
torch>=2.2.0

# Testing
pytest>=8.0.0
pytest-asyncio
